# Copy this file to .env and fill in real values. .env is gitignored.
# Postgres connection string (this project uses the pgvector/pgvector image).
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
